# じかん休
勤務時間と1つの休憩区間を設定し、出勤前・早退後の時間休を1時間単位で提案するアプリです。設定はブラウザに保存します。申請システムへの登録は行いません。

アプリ: https://jikan-kyuka-calculator.kabitaso.chatgpt.site/

## 計算方針
- 同日勤務のみ。休憩を除いた不在時間を、出勤前と早退後それぞれ60分単位で切り上げます。
- 出勤前は勤務開始、早退後は勤務終了を基点として、休憩を除いた提案区間を求めます。
- 端数によって実勤務と重なる部分は明示します。勤務時間を超える・区間が重なる提案は返しません。
- 全日休の換算など、職場独自のルールは実装していません。

## 開発
Node.js 24、pnpmを使用。`pnpm dev` / `pnpm build`。テストは `node --test tests/leave.test.ts`。

## WebMCP
対応ブラウザでは `calculate_leave_proposal` で画面の計算を更新できます。今回の環境では対応したWebMCP実行コンテキストがなく、実ブラウザでの契約検証は未実施です。
